// Package main: Config struct definitions (Server, TLS, Timeouts, Routes,
// Pools, HealthCheck, Backend), config parsing (via LoadConfig), default
// application, validation, and TLS certificate/key material resolution
// from local file paths. This is the single authoritative source for the
